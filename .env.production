# Committed on purpose, unlike .env — see the note there for why this is safe.
# Historically this is what the now-superseded Plesk build server read (see
# docs/plesk-deploy.md); Cloudflare Pages reads its own dashboard-configured
# Settings → Environment variables instead (see README.md's "Web → Cloudflare
# Pages" section) — kept in sync here anyway as the canonical production
# values, so there's one place to check rather than trusting the dashboard's
# copy is current. Keep this file in sync with .env whenever a value changes.

EXPO_PUBLIC_SUPABASE_URL=https://zsrhzjujoahresqfhpks.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_jsqMsYY3fmBTkIyrcD8E7g_ve9Ef1Ww
EXPO_PUBLIC_TELEGRAM_BOT_USERNAME=home_library_signin_bot
EXPO_PUBLIC_WEB_ORIGIN=https://app.kitobjavonim.uz
